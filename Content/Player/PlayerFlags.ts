import { Flags } from 'Engine/Flags';

export const PlayerFlags = Flags.register("Player", {
    PREGNANCY_CORRUPTION: 0,
    DOUBLE_ATTACK_STYLE: 0,
    SPELLS_CAST: 0,
    TIMES_GIVEN_BIRTH: 0,
});
